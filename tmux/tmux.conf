# Tmux Configuration

# Bind k to clear screen and history (like Cmd+K)
bind-key k send-keys C-l \; clear-history

# Optional: Make it work with uppercase K too
bind-key K send-keys C-l \; clear-history